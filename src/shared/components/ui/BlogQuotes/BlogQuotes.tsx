import styles from "./BlogQuotes.module.scss";

export const BlogQuotes = () => {
	return (
		<div className={styles.root}>
			<span>“</span>
			<h4>The details are not the details. They make the design.</h4>
		</div>
	);
};
